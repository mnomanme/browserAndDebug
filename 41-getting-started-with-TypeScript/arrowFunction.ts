// 41-6 Object types, type and arrow function type

function addTwo(num1: number, num2: number): number {
	return num1 + num2;
}

const addThree = (num1: number, num2: number): number => {
	return num1 + num2;
};

const doMath = (x: number, y: number): void => {};
