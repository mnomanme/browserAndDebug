"use strict";
// 41-6 Object types, type and arrow function type
function addTwo(num1, num2) {
    return num1 + num2;
}
const addThree = (num1, num2) => {
    return num1 + num2;
};
const doMath = (x, y) => { };
